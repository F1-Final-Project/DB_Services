const CategoryMenu = require('../models/category');class CategoryMenuService {    static async getAllCategoryMenu(){        return await CategoryMenu.find({});    }    static async getCategoryMenuById(id){        return await CategoryMenu.find({_id: id});    }    static async createCategoryMenu(data){        const newData = {            title: data.title,            description: data.description,            icon: String,        };        const createData = new CategoryMenu(newData);        await createData.save();    }    static async updateCategoryMenu(data, id){        const newData = {            title: data.title,            description: data.description,            icon: String,        };        await CategoryMenu.findOneAndUpdate({_id: id}, newData, {new: true});    }    static async deleteCategoryMenu(id){        await CategoryMenu.findByIdAndRemove(id);    }}module.exports = CategoryMenuService;