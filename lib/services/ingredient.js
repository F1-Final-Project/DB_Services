const Ingredient = require('../models/ingredient');class IngredientService {    static async getAllIngredient(){        return await Ingredient.find({});    }    static async getIngredientById(id){        return await Ingredient.findOne({_id: id});    }    static async createIngredient(data){        const newData = {            title: data.title,            restInStock: data.restInStock,            description: data.description,            price: data.price        };        const createData = new Ingredient(newData);        await createData.save();        return createData;    }    static async updateIngredient(data, id){        const newData = {            title: data.title,            restInStock: data.restInStock,            description: data.description,            price: data.price        };        await Ingredient.findOneAndUpdate({_id: id}, newData, {new: true});    }    static async deleteIngredient(id){        await Ingredient.findByIdAndRemove(id);    }};module.exports = IngredientService;