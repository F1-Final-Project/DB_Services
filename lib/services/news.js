const News = require('../models/news');class NewsService {    static async getAllNews(){        return await News.find({});    }    static async getNewsById(id){        return await News.find({_id: id});    }    static async createNews(data){        const newData = {            title: data.title,            text: data.text,            img: data.img        };        const createData = new News(newData);        await createData.save();    }    static async updateNews(data, id){        const newData = {            title: data.title,            text: data.text,            img: data.img        };        await News.findOneAndUpdate({_id: id}, newData, {new: true});    }    static async deleteNews(id){        await News.findByIdAndRemove(id);    }};module.exports = NewsService;